import { FiltroJugadoresPipe } from './filtro-jugadores.pipe';

describe('FiltroJugadoresPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroJugadoresPipe();
    expect(pipe).toBeTruthy();
  });
});
